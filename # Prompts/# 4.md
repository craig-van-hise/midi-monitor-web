**System Context & Objective:**
Final branding cleanup for the MIDI Monitor. The browser tab currently displays default AI Studio text. Your task is to update the application title and metadata.

**Task 1: Update Document Title**
* Locate `index.html` in the project root.
* Change the `<title>` tag content to: `VV | MIDI Monitor`.
* Additionally, locate the main entry point (e.g., `App.tsx` or `main.tsx`) and add `document.title = "VV | MIDI Monitor";` inside the main component's `useEffect` or initialization logic to ensure it persists.

**Task 2: Cleanup Metadata**
* Check `index.html` for any `<meta>` tags or comments referencing "Google AI Studio" and remove or update them to reflect the project name "VV MIDI Monitor".

**Task 3: Execute Git Commands**
* Stage the changes: `git add .`
* Commit the changes: `git commit -m "Update browser tab title and project branding"`
* Push the code: `git push origin main`

**Execution Command:**
Antigravity, update the title, commit, and push. Output the confirmation once the repo is updated.