**System Context & Objective:**
The initial GitHub Actions run failed with a 404 because it executed before the GitHub Pages environment was fully initialized. The user has now initialized the environment. Additionally, the runner threw a deprecation warning regarding Node.js 20. Your task is to update the workflow file to use Node.js 24 and push the commit, which will automatically trigger a fresh, successful deployment run.

**Task 1: Update Workflow YAML**
* Open `.github/workflows/deploy.yml`.
* Under the `Setup Node` step, change `node-version: 20` to `node-version: 24`.
* To explicitly satisfy the GitHub runner warning, add the required environment variable to the top of the workflow file (under the `on:` block but before `jobs:`), like this:
```yaml
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true