# Product Requirements Prompt (PRP) — BUG FIX: Visualizer Mode Sync — 🔘 Pending

## 1. Project Context & Objectives
**CRITICAL FAILURE IDENTIFIED:** The multi-mode visualization architecture implemented in the previous session is fundamentally broken. The UI correctly displays mode selection ("Controllers", "Notes", "Velocity"), but the Canvas render loop entirely ignores this state, continuing to render the "Controllers" (CC/Line Graph) mode exclusively. 

Your objective is to diagnose, audit, and fix the disconnect between the React state, the state reference, and the canvas render loop.

## 2. Technical Audit Directives
You must verify the following common points of failure in React/Canvas architectures:
1.  **Case Sensitivity/Value Mismatch:** Ensure the `onClick` handlers for the new UI buttons are setting the *exact* string literals: `'controllers'`, `'notes'`, `'velocity'`.
2.  **Stale Closure/Ref Sync:** Ensure `streamModeRef.current` is being properly updated via a `useEffect` hook whenever `streamMode` changes.
3.  **Render Loop Branching:** The `renderGraph` function must use strict `if / else if` blocks based on `streamModeRef.current` to isolate the rendering logic. 
4.  **Data Population:** Ensure `notesRef.current` is actually receiving data in the `onMidiMessage` function.

## 3. Implementation Phases

### Phase 1: State & UI Synchronization Audit — 🔘 Pending
* **Objective:** Ensure the React state correctly updates the ref used by the canvas loop.
* **Instructions:**
    1.  Inspect the declaration of `streamMode` and `streamModeRef` in `src/App.tsx`. 
    2.  **MANDATORY CHECK:** Verify the existence of: `useEffect(() => { streamModeRef.current = streamMode; }, [streamMode]);`. If it is missing or incorrect, fix it.
    3.  Inspect the UI buttons for the stream mode. Ensure their `onClick` handlers explicitly set the state to the exact lowercase strings: `setStreamMode('controllers')`, `setStreamMode('notes')`, and `setStreamMode('velocity')`.
* **TDD Checkpoint:** * **Test Case 1:** Click "NOTES" in the UI -> Assert `streamModeRef.current === 'notes'`.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Audit and fix the state sync. Once verified, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 2: Render Loop Isolation Fix — 🔘 Pending
* **Objective:** Fix the conditional branching inside the `renderGraph` function.
* **Instructions:**
    1.  Locate the `renderGraph` function inside the `useEffect` canvas hook.
    2.  Locate the logic that loops over `streams` (the line graph logic) and the legend rendering.
    3.  You **MUST** wrap the entire line-graph and legend drawing logic securely inside: `if (streamModeRef.current === 'controllers') { ... }`. 
    4.  Verify that the `else if (streamModeRef.current === 'notes')` and `else if (streamModeRef.current === 'velocity')` blocks are mutually exclusive and placed directly after the controllers block.
* **TDD Checkpoint:** * **Test Case 1:** Given `streamModeRef.current === 'notes'`, Assert the code block rendering line graphs (CCs) is completely bypassed.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Apply the strict conditional wrapping. Once verified, set status to ✅ Completed.

### Phase 3: Note Data Pipeline Verification — 🔘 Pending
* **Objective:** Ensure incoming MIDI notes are successfully populating the `notesRef` array so the canvas has data to draw.
* **Instructions:**
    1.  Locate the `onMidiMessage` handler.
    2.  Audit the `msg.isNoteOn` block. Ensure you are pushing an object to `notesRef.current` that strictly matches the `ActiveNote` type defined in the previous PRP.
    3.  Audit the memory management logic inside the `renderGraph` loop for notes/velocity. Ensure you are not accidentally filtering out active notes or improperly splicing the array, which would result in a blank screen.
* **HUMAN VALIDATION REQUIRED:** "STOP. Ask the user to run the application, connect their MIDI controller, and switch to 'Notes' mode. Ask them to verify that playing notes now visually populates the canvas with horizontal scrolling lines, and that CC lines no longer appear when 'Notes' is selected."
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Audit the data pipeline and halt for human validation. Once the user signs off, set status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Ensure no variables are undeclared and that all React dependencies are intact. 
* **Status Reconciliation:** Verify all sub-phases are ✅ Completed. Update the top-level PRP status to ✅ Completed.